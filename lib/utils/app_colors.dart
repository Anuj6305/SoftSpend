import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFFF9F9F9); // Very soft off-white
  static const Color primaryText = Color(0xFF1A1A1A); // Almost black
  static const Color secondaryText = Color(0xFF757575); // Grey
  static const Color accent = Color(0xFF4A6F5C); // Sage Green (Calm)
  static const Color white = Colors.white;
  static const Color lightGrey = Color(0xFFEEEEEE);

  // Dark Mode Palette
  static const Color darkBackground = Color(0xFF121212); // Soft dark background
  static const Color darkSurface = Color(
    0xFF1E1E1E,
  ); // Slightly lighter for cards
  static const Color darkPrimaryText = Color(
    0xFFE0E0E0,
  ); // Light grey (readable)
  static const Color darkSecondaryText = Color(0xFFA0A0A0); // Muted grey
  static const Color darkAccent = Color(
    0xFF6A8F7C,
  ); // Slightly brighter Sage for dark mode
}
